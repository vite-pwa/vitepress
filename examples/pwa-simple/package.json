{
  "name": "pwa-simple",
  "type": "module",
  "version": "0.0.0",
  "private": "true",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "npm run build && vitepress serve",
    "https": "npm run build && serve .vitepress/dist"
  },
  "devDependencies": {
    "@vite-pwa/vitepress": "workspace:*",
    "https-localhost": "^4.7.1",
    "typescript": "^4.8.3"
  }
}
